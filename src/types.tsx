export interface PropertiesOutputItemEdgeType {
  node: {
    id: string;
    name: string;
    forkCount: string;
    stargazerCount: string;
    url: string;
    description: string;
    issues: {
      totalCount: number;
    };
  };
}
